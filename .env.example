# Base URL of the BookList Pro API (the api-books-v2 folder, started via `npm start`).
# Copy this file to `.env` (or `.env.local` for an unversioned override).
EXPO_PUBLIC_API_URL=http://localhost:3000
